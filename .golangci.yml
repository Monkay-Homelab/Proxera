version: "2"

linters:
  default: none
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - gosec

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

run:
  go: "1.26"
  timeout: 5m
